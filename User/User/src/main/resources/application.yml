server:
  port: 8082

spring:
  application:
    name: User-service

  datasource:
    url: jdbc:mysql://localhost:3306/User_db
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true